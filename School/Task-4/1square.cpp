#include <iostream>

using namespace std;

int main()
{
    int height, width;

    // PROGRAM
    cout<<"Input the height value = ";
    cin>>height;

    cout<<endl<<"Input the widht value = ";
    cin>>width;

    cout<<endl;
    cout<<"Square volume is = "<<height * width;
    return 0;
}